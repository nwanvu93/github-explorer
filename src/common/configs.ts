const Configs = {
    BASE_URL: 'https://api.github.com',
    PARAM: {
        PAGE: 'page',
        PAGE_SIZE: 'per_page',
        QUERY: 'q'
    },
    PAGE_SIZE: 30,
} as const

export default Configs;